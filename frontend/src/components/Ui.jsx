import {Search} from '../icons'; export function PageHead({eyebrow='Clevia CRM',title,subtitle,action}){return <div className="page-head"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div>{action}</div>} export function Stat({icon:Icon,label,value,meta}){return <div className="stat-card"><span className="stat-icon"><Icon/></span><div><small>{label}</small><strong>{value}</strong><em>{meta}</em></div></div>} export function SearchBox({value,onChange,placeholder='Search...'}){return <label className="searchbox"><Search/><input value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder}/></label>} export function Badge({children}){const c=String(children||'').toLowerCase().replaceAll('_','-').replaceAll(' ','-');return <span className={`badge badge--${c}`}>{String(children||'').replaceAll('_',' ')}</span>}