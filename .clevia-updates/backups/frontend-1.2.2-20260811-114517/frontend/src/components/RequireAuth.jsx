import {Navigate,useLocation} from 'react-router-dom'; import {useAuth} from '../context/AuthContext'; export default function RequireAuth({children}){const {user,loading}=useAuth(),loc=useLocation();if(loading)return <div className="fullscreen-loader"><span/></div>;if(!user)return <Navigate to="/admin/login" replace state={{from:loc}}/>;return children;}