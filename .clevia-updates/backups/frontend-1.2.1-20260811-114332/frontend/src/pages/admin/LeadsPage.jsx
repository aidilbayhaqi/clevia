import {Plus} from 'lucide-react';import {useEffect,useState} from 'react';import {crmApi} from '../../api/crmApi';import {Badge,PageHead,SearchBox} from '../../components/Ui';import {dateTimeID} from '../../utils/format';export default function LeadsPage(){const [items,setItems]=useState([]),[q,setQ]=useState('');useEffect(()=>{crmApi.leads().then(setItems)},[]);const rows=items.filter(x=>(x.name+x.phone+x.interest).toLowerCase().includes(q.toLowerCase()));return <><PageHead title="Leads" subtitle="Track potential clients from web, WhatsApp, and other acquisition channels." action={<button className="btn btn--admin"><Plus/> New lead</button>}/><section className="panel"><div className="table-tools"><SearchBox value={q} onChange={setQ} placeholder="Search leads..."/><span>{rows.length} records</span></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Interest</th><th>Source</th><th>Created</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><b>{x.name}</b><small>{x.phone}</small></td><td>{x.interest}</td><td>{x.source}</td><td>{dateTimeID(x.created_at)}</td><td><Badge>{x.status}</Badge></td></tr>)}</tbody></table></div></section></>}