import {ArrowRight,LockKeyhole,Sparkles} from 'lucide-react';import {useState} from 'react';import {Navigate,useLocation,useNavigate} from 'react-router-dom';import Logo from '../../components/Logo';import {useAuth} from '../../context/AuthContext';import {DEMO_MODE} from '../../config';export default function LoginPage(){const {user,login}=useAuth(),nav=useNavigate(),loc=useLocation(),[email,setEmail]=useState('owner@clevia.local'),[password,setPassword]=useState('ChangeMe123!'),[error,setError]=useState(''),[loading,setLoading]=useState(false);if(user)return <Navigate to="/admin" replace/>;async function submit(e){e.preventDefault();setLoading(true);setError('');try{await login(email,password);nav(loc.state?.from?.pathname||'/admin',{replace:true})}catch(e){setError(e.message)}finally{setLoading(false)}}return <div className="login-page"><div className="login-visual"><Logo dark/><div><span className="pill pill--dark"><Sparkles/> Clevia operations</span><h1>One workspace.<br/><em>A calmer clinic.</em></h1><p>Manage leads, appointments, conversations, and AI-assisted customer journeys without losing human control.</p></div><small>Internal access only · Clevia Beauty Clinic</small></div><div className="login-form-wrap"><form onSubmit={submit} className="login-form"><span className="login-lock"><LockKeyhole/></span><small>ADMIN PORTAL</small><h2>Welcome back.</h2><p>Sign in to continue to Clevia CRM.</p>{error&&<div className="alert">{error}</div>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={loading} className="btn btn--dark btn--wide">{loading?'Signing in...':'Sign in'} <ArrowRight/></button>{DEMO_MODE&&<div className="demo-hint"><b>Demo Mode</b><span>owner@clevia.local</span><span>ChangeMe123!</span></div>}</form></div></div>}